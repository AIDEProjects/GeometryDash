✓解决绘制边缘越界问题
✓基本解决碰撞问题：包括卡边(使用deathzone范围留出忽略距离)，穿透(同时碰到两条边，判定到线外，背线，同法向线，超距离线)
✓增加一个按钮来启用/禁用debugtxt区域的交互
✓解决斜着走会穿透的问题(collDeathZone为0时可以解决，但是会有蹭边问题)
✓可视化绘制collDeathZone并使用SeekBar调整大小
解决collDeathZone的判定逻辑问题，真正实用化
解决debugTxt视图高频刷屏会卡死问题
debugtxt更新为上持续区，下日志区